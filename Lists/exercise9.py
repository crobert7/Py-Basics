userWord = input('Type a word: ')

for letra in userWord:
    if letra == 'a':
        continue
    if letra == 'e':
        continue
    if letra == 'i':
        continue
    if letra == 'o':
        continue
    if letra == 'u':
        continue
    else: 
        print(letra.upper())